{
  "name": "registro-privado-firebase",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/unit.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-registro 'node --test tests/rules.test.mjs'",
    "authorize": "node scripts/authorize.mjs"
  },
  "dependencies": { "firebase": "12.18.0" },
  "devDependencies": { "vite": "8.0.13", "firebase-admin": "^13.0.0", "@firebase/rules-unit-testing": "^5.0.0", "firebase-tools": "14.12.0" }
}
